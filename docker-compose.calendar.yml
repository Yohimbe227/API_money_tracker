version: '3.3'

services:
  backend:
    image: yohimbe/calendar_backend:v2
    environment:
      - PRIVATE_KEY=key.json
      -
    env_file:
      - .env.calendar
    volumes:
      - ./key.json:/key.json
      - ./diagram:/app
      - ./home/calendar/.env.calendar:/app/.env.calendar


#  nginx:
#    image: nginx:1.18.0-alpine
#    ports:
#      - "80:80"
#    volumes:
#      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
#      - static_value:/var/html/static/
#      - media_value:/var/html/media/

